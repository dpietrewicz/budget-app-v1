import React from "react";

const Header = () => {
    return (
        <div
            className="logo"
            style={{ backgroundImage: "url(public/assets/budget.png)" }}
        ></div>
    );
};

export default Header;
